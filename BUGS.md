| BR-1 | Краткое описание | При создании объявления (POST /api/1/item) сервер возвращает только строку {"status":"Сохранили объявление - <uuid>"}, в то время как документация (в виде Postman-коллекции) ожидает объект объявления с полями id, sellerId, name, price, statistics, createdAt. |
| Шаги воспроизведения | 1.Отправить POST-запрос на https://qa-internship.avito.com/api/1/item с валидным JSON-телом. |
| Фактический результат | Ответ содержит только поле status со строкой, содержащей UUID. |
| Ожидаемый результат | Ответ должен содержать полный объект созданного объявления в соответствии со спецификацией. |
| Серьёзность | P1: нарушение контракта, но объявление создаётся и ID доступно для дальнейших операций. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-2 | Краткое описание | Поле createdAt в ответе GET /api/1/item/{id} возвращается в формате "2026-03-24 10:49:26.781703 +0300 +0300" – временная зона повторена дважды, что не соответствует ни одному временному стандарту. |
| Шаги воспроизведения | 1.Создать объявление через POST.2.Получить его через GET /api/1/item/{id}.3.Проверить значение поля createdAt. |
| Фактический результат | Строка даты содержит два смещения |
| Ожидаемый результат | Поле должно соответствовать временному стандарту спецификации (например ISO 8601) или хотя бы быть с одним смещением. |
| Серьёзность | P2: нарушение формата, не влияет на функционал |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-3 | Краткое описание | При отправке POST-запроса на создание объявления, если в теле JSON присутствуют поля с различием лишь в регистре (например, sellerID и SellerID, statistics и Statistics), сервер не возвращает ошибку, а игнорирует дублирующиеся поля и создаёт объявление. Это может привести к неоднозначности и потенциальной потере данных. |
| Шаги воспроизведения | 1. Сформировать JSON, содержащий поля sellerID и SellerID с разными значениями, а также statistics и Statistics с разными значениями внутри.2. Отправить POST-запрос на /api/1/item с этим JSON.3. Получить созданное объявление через GET /api/1/item/{id} и проверить, какие значения были сохранены. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление.В ответе GET присутствуют поля только с корректным регистром. Значения полей могут быть взяты как из полей с правильным регистром, так и с неправильным (например, из sELLerID, а не SellerID). |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление.В ответе GET присутствуют поля только с корректным регистром. Значения взяты из полей с правильным регистром (например, из sellerID, а не SellerID).Лишние поля игнорируются. |
| Серьёзность | P2: неопределенное поведение |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-4 | Краткое описание | При создании объявления с sellerID = 0 сервер возвращает 400 Bad Request, хотя по логике поля значение 0 должно быть валидным. |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить sellerID = 02. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что sellerID - обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Серьёзность | P1: нарушение валидации |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-5 | Краткое описание | При создании объявления с price = 0 сервер возвращает 400 Bad Request, хотя по логике поля значение 0 должно быть валидным. |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить price = 02. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что price - обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Серьёзность | P1: нарушение валидации |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-6 | Краткое описание | При создании объявления с likes = 0 сервер возвращает 400 Bad Request, хотя по логике поля значение 0 должно быть валидным. |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить likes = 02. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что likes- обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление.В ответе присутствуют все переданные поля |
| Серьёзность | P1: нарушение валидации |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-7 | Краткое описание | При создании объявления с viewCount = 0 сервер возвращает 400 Bad Request, хотя по логике поля значение 0 должно быть валидным. |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить viewCount = 02. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что viewCount - обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Серьёзность | P1: нарушение валидации |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-8 | Краткое описание | При создании объявления с contacts = 0 сервер возвращает 400 Bad Request, хотя по логике поля значение 0 должно быть валидным. |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить contacts = 02. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что contacts - обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Серьёзность | P1: нарушение валидации |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-9 | Краткое описание | При создании объявления с sellerID < 0 сервер возвращает 400 Bad Request, хотя по логике поля значения меньше 0 должны быть невалидными |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить отрицательное значение sellerID2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что sellerID - неотрицательное поле |
| Серьёзность | P1: нарушение валидации, позволяет сохранять некорректные данные. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-10 | Краткое описание | При создании объявления с price < 0 сервер возвращает 400 Bad Request, хотя по логике поля значения меньше 0 должны быть невалидными |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить отрицательное значение price2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что price - неотрицательное поле |
| Серьёзность | P1: нарушение валидации, позволяет сохранять некорректные данные. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-11 | Краткое описание | При создании объявления с likes < 0 сервер возвращает 400 Bad Request, хотя по логике поля значения меньше 0 должны быть невалидными |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить отрицательное значение likes2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что likes - неотрицательное поле |
| Серьёзность | P1: нарушение валидации, позволяет сохранять некорректные данные. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-12 | Краткое описание | При создании объявления с viewCount < 0 сервер возвращает 400 Bad Request, хотя по логике поля значения меньше 0 должны быть невалидными |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить отрицательное значение viewCount2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что viewCount - неотрицательное поле |
| Серьёзность | P1: нарушение валидации, позволяет сохранять некорректные данные. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-13 | Краткое описание | При создании объявления с contacts < 0 сервер возвращает 400 Bad Request, хотя по логике поля значения меньше 0 должны быть невалидными |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в котором установить отрицательное значение contacts2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 200 и создаёт объявление с переданными полями |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что contacts - неотрицательное поле |
| Серьёзность | P1: нарушение валидации, позволяет сохранять некорректные данные. |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-14 | Краткое описание | Сообщение об ошибке при отсутствии поля statistics не на отсутствие статистики, а на отсутствие поля внутри него |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, поле statistics не указывать2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что likes - обязательное поле |
| Ожидаемый результат | Сервер возвращает статус 400, ошибка в ответе сообщает, что statistics - обязательное поле |
| Серьёзность | P2: вводит в заблуждение, функциональность не нарушена |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-15 | Краткое описание | В ответе POST при неверном типе данных значение поля message записано в поле status |
| Шаги воспроизведения | 1. Сформировать JSON с валидными полями, в случайное поле поставить не предусмотренный тип данных2. Отправить POST-запрос на https://qa-internship.avito.com/api/1/item3. Проверить ответ. |
| Фактический результат | Сервер возвращает статус 400, в ответе поле message - пустое, поле status сообщает о непереданном теле |
| Ожидаемый результат | Сервер возвращает статус 400, в ответе поле message - сообщение о неверном типе соответствующего поля, поле status передает статус ошибки |
| Серьёзность | P2:вводит в заблуждение, функциональность не нарушена |
| Окружение | API https://qa-internship.avito.com, метод POST /api/1/item |

***

| BR-16 | Краткое описание | Спецификация для ответа 404 в GET /api/1/item/{id} и GET /api/1/statistic/{id} не в полном объеме отображают формат |
| Шаги воспроизведения | 1.Сгенерировать заведомо несуществующий UUID (например, 00000000-0000-0000-0000-000000000000).2. Отправить GET-запрос на https://qa-internship.avito.com/api/1/item/{id}.3. Отправить GET-запрос на https://qa-internship.avito.com/api/1/statistic/{id}.4. Сравнить полученные ответы с описанием в спецификации (Postman-коллекции) |
| Фактический результат | В документации ответ с структурой{"result": "laborum","status": "cillum enim eiusmod"} |
| Ожидаемый результат | В документации единообразный ответ с структурой{"result": {"messages": {"culpa_b92": "<string>","enim_24f": "<string>","mollit_aa": "<string>"},"message": "<string>"},"status": "<string>"} |
| Серьёзность | P2: ошибка в документации, не влияет на функциональность API, вводит в заблуждение |
| Окружение | спецификация (Postman-коллекция) |

***

| BR-18 | Краткое описание | Неединообразные ответы 400 для эндпоинтов /item и /statistic |
| Шаги воспроизведения | 1.Сгенерировать невалидную строку в качестве id2. Отправить GET-запрос на https://qa-internship.avito.com/api/1/item/{id}.3. Отправить GET-запрос на https://qa-internship.avito.com/api/1/statistic/{id}.4. Сравнить полученные ответы |
| Фактический результат | Для /item/{id} в ответе в поле "message" возвращается сообщение "ID айтема не UUID:{id}".Для /statistic/{id} в ответе в поле "message" возвращается сообщение "передан некорректный идентификатор объявления". |
| Ожидаемый результат | Единый формат ошибки в одинаковых ситуациях |
| Серьёзность | P2 – несоответствие контракта |
| Окружение | API https://qa-internship.avito.com, метод GET /api/1/statistic/{id} и GET /api/1/item/{id} |

***

| BR-19 | Краткое описание | GET /item/ с пустым путём возвращают 404, в поле "status" ответа 400 |
| Шаги воспроизведения | 1. Отправить GET-запрос на https://qa-internship.avito.com/api/1/item/2. Проверить ответ |
| Фактический результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа не соответствует HTTP-статусу ("status":"400") |
| Ожидаемый результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа соответствует HTTP-статусу ("status":"404") |
| Серьёзность | P2: некорректный статус, функциональность не блокируется. |
| Окружение | API https://qa-internship.avito.com, метод GET /api/1/item/{id} |

***

| BR-20 | Краткое описание | GET /statistic/ с пустым путём возвращают 404, в поле "status" ответа 400 |
| Шаги воспроизведения | 1. Отправить GET-запрос на https://qa-internship.avito.com/api/1/statistic/2. Проверить ответ |
| Фактический результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа не соответствует HTTP-статусу ("status":"400") |
| Ожидаемый результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа соответствует HTTP-статусу ("status":"404") |
| Серьёзность | P2: некорректный статус, функциональность не блокируется. |
| Окружение | API https://qa-internship.avito.com, метод GET /api/1/statistic/{id} |

***

| BR-21 | Краткое описание | При отправке DELETE на несуществующее объявление сервер 404 Not Found, но в поле "status" ответа 400 |
| Шаги воспроизведения | 1.Сгенерировать заведомо несуществующий UUID (например, 00000000-0000-0000-0000-000000000000).2. Отправить DELETE-запрос на https://qa-internship.avito.com/api/2/item/{id}.3. Проверить ответ |
| Фактический результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа не соответствует HTTP-статусу ("status":"500") |
| Ожидаемый результат | HTTP-статус: 404 Not Found.Поле "status" в теле ответа соответствует HTTP-статусу ("status":"404") |
| Серьёзность | P2: некорректный статус, функциональность не блокируется. |
| Окружение | API https://qa-internship.avito.com, метод Delete 2/item/{id} |